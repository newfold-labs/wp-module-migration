{
  "name": "@newfold-labs/wp-module-migration",
  "description": "The migration module is used to initiate the migration process by installing the required plugins for migration.",
  "license": "GPL-3.0-or-later",
  "private": true,
  "scripts": {
    "set-version-bump": "node ./set-version-bump.js && composer i && composer run i18n"
  },
  "dependencies": {},
  "devDependencies": {}
}
